package cmd

import (
	"fmt"

	"github.com/Artiii4/calculations/for3Lab/pkg"
)

func main() {
	//Calculate(2, false)
	fmt.Println(pkg.Calculate(2, false))
	//customFuncs.calculations.Calculate(3,true)
	//calculations.Calculate(2, false)
	//customFuncs.calculations.Calculate(2, false)
	//fmt.Println(Calculate(2, false))
	fmt.Println("GG")
}
